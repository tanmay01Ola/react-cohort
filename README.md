# bun-react-template

To install dependencies:

```bash
bun install
```

To start a development server:

```bash
bun dev
```

To run for production:

```bash
bun start
```

This project was created using `bun init` in bun v1.3.12. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
<<<<<<< HEAD

here i learnt about useEfect and dependency array used in it ... UseEffect is used to do side effects like fetching from be . And useEffect only runs once when the components mounts it didn't run on every time the function render . We pass dependancy array in useEffect if we want useEfect to run again .I f something we passed in useEffect changes then it will run again
>>>>>>> 69fa0e6 (created todo app)
